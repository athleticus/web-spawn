{
  "name": "web-spawn",
  "version": "1.0.0",
  "description": "Provides communication and interaction with server-side processes from the client-side through web sockets.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "node bin/min"
  },
  "keywords": [
    "socket",
    "spawn",
    "web"
  ],
  "author": "Ben Martin",
  "license": "MIT",
  "dependencies": {
    "node-minify": "^1.1.0"
  }
}
